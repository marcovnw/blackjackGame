{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.24.0"
  },
  "scripts": {
    "deploy": "npx hardhat ignition deploy ./ignition/modules/Lock.ts --network localhost"
  }
}
